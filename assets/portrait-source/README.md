# Portrait source intermediates

Working files from portrait cutout processing. **Not served.**

Live assets (committed under `public/`):

- `portrait-cutout.png` — hero
- `portrait.png` — fallback

Keep originals out of `public/` so they are not downloadable.
